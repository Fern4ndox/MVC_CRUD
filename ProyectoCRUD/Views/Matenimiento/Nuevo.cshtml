@model ProyectoCRUD.Models.ViewModels.MantenimientoViewModel
@{
    ViewBag.Title = "Nuevo";
}

<h2>Nuevo</h2>
<!-- usamos helper para crear el formulario de tipo Post  -->
@using (Html.BeginForm("Nuevo", "Matenimiento", FormMethod.Post))
{
    <div class="row">
        <div class="col-md-12">
            <div class="form-group">
                <!-- Creo label   -->
                @Html.LabelFor(p => p.Descripcion):
                <!-- Creo textbox  -->
                @Html.TextBoxFor(p => p.Descripcion, "", new { @class = "form-control" })
                <!-- para mostrar mensaje de error  -->
                @Html.ValidationMessageFor(p => p.Descripcion, null, new { @class = "text-danger" })
            </div>
            <div class="form-group">
                <!-- Creo label   -->
                @Html.LabelFor(p => p.KM):
                <!-- Creo textbox  -->
                @Html.TextBoxFor(p => p.KM, "", new { @class = "form-control" })
                <!-- para mostrar mensaje de error  -->
                @Html.ValidationMessageFor(p => p.KM, null, new { @class = "text-danger" })
            </div>
            <div style="text-align:center">
                <input type="submit" class="btn btn-primary" value="Guardar" />
            </div>
        </div>
    </div>
}